<%= form_with(model: method_payment, local: true) do |form| %>
  <% if method_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(method_payment.errors.count, "error") %> prohibited this method_payment from being saved:</h2>

      <ul>
      <% method_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :method, value: 'Метод' %>
    <div class="row">
      <div class="col-2">
        <%= form.text_field :method, id: :method_payment_method, class: 'form-control' %>
      </div>
    </div>    
  </div>

  <div class="form-group">
    <%= form.submit class: 'btn btn-dark', value: 'Сохранить' %>
    <%= link_to 'Назад', method_payments_path, class: 'btn btn-outline-dark' %>
  </div>
<% end %>
